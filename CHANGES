Revision history for File::Walker:

2004-01-31 v0.01 Tels 126 tests
  * first version with:
    + new()
    + step(), reset()
    + start(), current(), curdir(), level(), reset(),
    + dirs(), files(), steps()
